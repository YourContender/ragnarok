export interface Editions {
	id: number;
	img: string;
	title: string;
	subtitle: string;
	descr: string[];
	price: string;
}

export interface ExploreTypes {
	cpu: string;
	ram: string;
	os: string;
	videocard: string;
	pixel: string;
	vertex: string;
	disk: string;
	video: string;
}
